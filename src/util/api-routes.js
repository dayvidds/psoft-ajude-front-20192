export const API_URI = 'http://localhost:8080';

const API_URL = `${API_URI}/api/v1`;

export const API_URL_CADASTRO = `${API_URL}/usuario/cadastro`;
export const API_URL_LOGIN = `${API_URL}/autorizacao/login`;

export default API_URL;
